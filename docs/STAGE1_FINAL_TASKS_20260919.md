# Stage1接最终任务联合适配与两头精修

2026-09-19。用户明确选择“最终语义＋affinity联合适配，再分别精修”。
这取代旧joint-v3语义/边界微调，不是仅删除joint-v3的单变量消融。
本实验不读取joint-v3、V6、E10a或既有G系列权重；不改变已验证部署默认入口。

当前状态：2026-09-19用户回报本轮官方`0.8448/0.7699/80.735`，较统一组合低4.190分，拒绝晋级。
联合适配e35、几何精修e57、语义精修e14及完整预算均已核验；本次整套joint-v3替代未通过，
不能由此证明joint-v3本身不可替代。保留统一组合84.925为研究对照、原S 85.535为最高分回退。

## 官方回分与归因边界

分数来自用户回报，按本任务提交顺序归属于文末s14/g57提交包；未另取得平台包哈希回执。
总分按`50*(mIoU+面积项)`计算。

| 方案 | mIoU | 面积项 | 总分 |
| --- | ---: | ---: | ---: |
| S-align+V统一组合 | 0.8416 | 0.8569 | 84.925 |
| Stage1最终任务替代 | 0.8448 | 0.7699 | 80.735 |
| 本轮减统一组合 | +0.0032 | −0.0870 | −4.190 |
| 原S最高分回退 | 0.8393 | 0.8714 | 85.535 |

mIoU贡献+0.160分，面积贡献−4.350分；本轮较原S低4.800分。
这个代价不接受为简化流程的工程取舍，也不能凭mIoU略升或内部面积代理改善继续晋级。
赛题记录中的mIoU只平均同类且IoU≥0.5的有效匹配；漏配/合并或类别变化可能改变入选集合，
因此mIoU提高不证明实例召回或拓扑改善。没有官方逐图匹配与面积误差，不能指定其上涨的唯一原因。

本轮同时替换LoRA适配任务、语义/几何头初始化与训练过程、语义学习率及固定教师依赖；
旧joint-v3还包含Stage1固定边界缓存上的无标签监督，本替代未保留该环节。
现有证据只否决整套替代配方，不能把退步单独归因于删joint-v3、新GT、共享LoRA或某个任务头。
6600次更新均执行，导出/共享特征契约通过；没有证据用训练未完成、格式错误或缺键加载解释此次回分。

## 回分后的本地输出诊断

本轮只读取已有两份提交包，不连接服务器、不新训、不修改推理或阈值，也不构造测试GT。
预测平均面积增加发生于52/68图：31图超过+10%、12图超过+20%；中位+8.87%，最大+58.32%。
这说明输出变化广泛，不能仅用一两张输出离群图概括；但不能据此量化各图对官方掉分的贡献。

为检查类别变化，按形状IoU≥0.5做不限定类别的一对一旧/新预测对应，共4966对。
这是预测间比较，两边都不是GT，也不是对两个头的因果消融。

| 对应结果 | 数量 |
| --- | ---: |
| 旧铁素体 → 新珠光体 | 308 |
| 旧珠光体 → 新铁素体 | 17 |
| 对应实例的净铁素体变化 | −291 |
| 未能对应部分的净铁素体变化 | −278 |
| 全部铁素体变化 | −569（4283→3714） |

旧铁素体→新珠光体这308个实例，旧预测面积中位数1575像素；保持铁素体的3330个对应实例为14950.5像素。
前者旧预测面积合计1,150,446像素，约占旧预测铁素体总像素0.90%。
较小区域的类别改变对像素指标可能很轻，却显著改变铁素体平均面积分母；不能只检查affinity合并。
上述−291/−278是计数恒等分解，不是“语义造成51%、几何造成49%”的因果分摊：
形状变化会改变类别投票范围，未对应部分也可能混合合并、分裂、边界位移和类别改变。
更不能在没有GT时把308次改判直接称为错误。

私有复现：`output/20260919_stage1_final_tasks/analyze_blackbox_shift.py`及`blackbox_shift_analysis.json`。
后续优先考虑固定权重的两头输出交换对照，见文末；暂不延长训练、继续取消Stage1或加入EMA。

比较基准补充（2026-09-19）：用户已回报[S-align+V统一组合](UNIFIED_ALIGNMENT_AUDIT_20260919.md)
官方`0.8416/0.8569/84.925`。本实验完成后，按预定损失选优、固定top2/high0.65等完整部署方式，
与该统一组合比较，同时报告相对原S最高总分85.535的差距。该回分不改变下述训练预算、学习率或选优规则，
也不作为本实验已经成功替代joint-v3的证据。

## 训练与选优

| 环节 | 更新参数 | 数据和预算 | 学习率 | 选优 |
| --- | --- | --- | --- | --- |
| Stage1初始化 | 无 | 复用既有Stage1；LoRA96张量、语义coarse、边界FPN严格加载 | — | 既有Stage1 best |
| 新输出层适应 | 两个任务头，LoRA冻结 | 5轮；每轮64个人工批次＋64个SAM2批次，batch1 | 语义/affinity各1e-4 | 新GT语义loss＋旧GT人工affinity loss最小 |
| 最终任务联合适配 | 两头＋LoRA；SAM2原始参数冻结 | 30轮；同采样，1920次计划更新 | 语义3e-5、affinity5e-5、LoRA2e-6，余弦降至1e-6 | 同上，阶段内选优 |
| affinity精修 | 仅affinity，冻结适配后的共享特征 | 120轮；旧人工26/6划分＋64源图SAM2，52次抽取/轮、batch2，共3120次计划更新 | FPN5e-5、新层1e-4，最低5e-6 | 既有人工验证affinity loss |
| 语义精修 | 仅语义，冻结同一共享特征 | 20轮；补缝新GT25/7，62步/轮，共1240次计划更新 | 4e-5；沿用既有warmup/flat/decay | 新GT验证语义loss |

两个人工任务loss直接相加，权重均为1；SAM2 affinity loss权重1，与人工affinity等权。
SAM2没有类别监督，未覆盖及covered-uncovered pair忽略；人工affinity未引入G4b的旧缝隙负边项。
每阶段从预先定义的损失最佳权重交接，不按内部实例/面积代理或monitor图选轮。
阶段1到阶段2先恢复head-warmup loss-best；两个最终精修任务均从同一个joint-lora loss-best开始。
总共计划6600次优化器更新，不以删除阶段为由缩短随机层所需训练预算。

语义在本实验中继承Stage1 coarse，新增高分辨率模块从默认零输出开始；最终精修不再重置语义头。
affinity继承Stage1边界FPN，新增上采样及affinity输出随机初始化。
共享LoRA更新后，所有下游头都在新特征上重新适配，没有直接挂接旧S/V头。

## 数据与解释边界

- 联合阶段采用既有语义25/7划分；同一图的新GT用于语义、原LabelMe用于affinity。
  语义类别、语义实例、有效域与辅助边界全部从新GT同一实例图派生，不混入旧语义/实例监督。
- 两个任务共享同一次翻转/旋转；unknown及reflect补边域不参加新GT语义监督。
- affinity最终训练保留原26/6划分；实际manifest核对确认，6张几何验证图全部进入联合阶段训练，
  7张语义验证图也全部进入后续几何精修训练；不能把几何验证loss降低解释为严格留出集上的泛化收益。
  既有Stage1也有自己的训练历史。因此内部7图/6图不是全系统未见留出集，不能合并冒充13图独立验证。
- 不使用旧固定语义教师；语义精修将`distill_weight_start/end`同时设为0。
  本轮没有引入EMA或1000图上的新在线一致性；仍复用历史SSL并使用已批准SAM2几何监督。
- 相比S-align+V，本实验同时替换共享特征训练、两头初始化及旧教师依赖，语义精修采用适合已有头的学习率。
  若成功，支持整套替代流程可行；若失败，不能单独断言joint-v3不可替代或Stage1无效。

## 实现与运行

入口：

```bash
python tools/run_stage1_final_tasks.py --smoke
python tools/run_stage1_final_tasks.py
```

启动器串行执行任务适配、权重导出及前向核对、120轮affinity、20轮语义。任一步失败即停止后续。
`--start-at export|geometry|semantic`可显式从指定步骤继续；适配中断使用`--resume-adaptation`指定latest。
适配保存优化器、scheduler、AMP scaler及主进程/采样器随机状态，num_workers=0。
只保留阶段best/latest和最终产物，不生成逐轮监控图。

- 适配配置：`config/train/stage1_final_tasks_20260919.yaml`
- 语义精修：`config/train/stage1_final_semantic20_20260919.yaml`
- 几何精修：`config/train/stage1_final_geometry120_20260919.yaml`
- 最终推理：`config/experiments/stage1_final_tasks_top2_20260919.yaml`

推理保持legacy_none、top2、高阈值0.65、seal2、局部重建8、受阻分水岭及probability_mean。
新部署配置的全部inference项与S-align+V相同，只有输出路径不同；权重及参考载体替换为本实验。

新服务器端口15930，隔离运行目录：
`/root/autodl-tmp/segmentationv2_stage1_tasks_20260919`。
产物根目录：`outputs/20260919_stage1_final_tasks`；`pipeline_status.json`记录当前步骤及异常。
保留原始Stage1文件，只导出decoder/LoRA/config等必要项，迁移52,449,521 bytes。
源checkpoint的epoch字段为80；原文件SHA256为
`62037119de3a3628941c86fd04bf8a51858ce30f8b05e138eb3cd2dc7eb01c2f`，
去除优化器等状态后的迁移文件SHA256为
`4a0bb609eacdfd6af7feea3db962a966f9506e814863880c3faaa5475efb4298`。
64份SAM2 mask与旧运行逐文件相同；两份manifest只有generated_at不同，源图hash相同，复用已有用户批准记录。
基础权重、图像和新GT复用服务器现有资产；产物始终保留在忽略目录，不提交Git/GitHub。

## 验证和状态

本地19项相关测试通过：GT来源隔离、unknown梯度、Stage1完整初始化、旧路径兼容、AMP更新计数等。
真实GPU短跑已验证适配后导出前后语义/affinity logits最大绝对差均为0。
完整串行短跑通过：预热和联合各1次真实更新、AMP跳步0；预热LoRA最大变化0，
联合阶段LoRA梯度范数1.554、最大参数变化约1.997e-6。两个最终训练器各完成1轮，
语义精修后的LoRA与适配导出逐张量同值。额外1图完整推理通过原生尺寸、单通道uint16及ID上限检查。
这些仅是功能检查，短跑随机affinity尚未学会分割，不能用其图像/实例数判断质量。
验证完成后已删除11份短跑权重及其副本，共701,048,201 bytes；保留日志、CSV和验证摘要。
正式训练不依赖这些短跑文件，正式阶段best/latest与最终产物继续保留。

正式串行训练已于2026-09-19 10:25:03（服务器本地时区）启动，初始父进程PID为7409。
日志为产物根目录的`pipeline.log`；`launch.json`保存启动回执。
启动时确认正式预热第1轮完成64/64次实际更新、AMP跳步0、LoRA最大变化0，随后停止持续追踪。
用户通知完成后核验：2026-09-19 12:45:34结束，约2小时20分30秒；随后官方回分80.735，见文首。

固定教师项权重为0，既有语义训练器仍可计算无标签诊断量；日志中非零的`s_c`不表示该项参与梯度目标。
无标签图的读取不应被表述为本轮新增了有效的1000图一致性监督。

## 完成后核验与损失

`pipeline_status.json`为`completed`，adaptation/export/geometry/semantic全部成功。
5轮预热320次、30轮联合1920次、120轮几何3120次、20轮语义1240次，共6600次实际更新；AMP跳步全部为0。
预热全部轮次LoRA变化为0，联合适配末轮相对Stage1的LoRA最大变化为0.001455；语义精修全程LoRA变化为0。
正式导出前后语义及affinity logits最大绝对差均为0；最终语义与导出参考的96个LoRA张量逐张量相同，
最终部署的几何/共享语义来源契约为`exact`。加载未出现需要忽略的架构缺键。

| 环节 | loss选优轮次 | 最低验证loss | 补充 |
| --- | ---: | ---: | --- |
| 两头预热 | 4/5 | 0.532034 | 语义与人工affinity验证loss之和 |
| 联合LoRA | 总e35，即联合30/30 | 0.342389 | 同上；e28已为0.342510，不能仅凭末轮最佳推断明显欠训练 |
| affinity精修 | 57/120 | 0.189491 | V为0.202752，但本轮六图已参加联合训练；末轮0.199103 |
| 语义精修 | 14/20，checkpoint字段13 | 0.063863 | S-align为0.069422；e14训练验证像素mIoU为0.952293 |

语义checkpoint中的`best_score=0.952560`是历轮mIoU最大值，不是loss-best e14的mIoU；不按它重新选轮。
本轮没有改变既定训练预算、学习率、损失选优或任何推理阈值。

## 固定最终部署对照

配置：`config/experiments/stage1_final_tasks_top2_20260919.yaml`。
与S-align+V使用相同最终推理代码、全部inference参数（输出路径除外）、top2融合参数和新GT评估域；
GT manifest SHA256为`5f4af78b746b22e7bffcd41fcb7d995ef9ad7f2a1f1591e3c2942926ba73d753`。
在当前运行环境复现旧组合的一张图，最终实例像素及类别JSON与缓存完全一致。
以下是内部诊断，两个样本组均非全链未见留出集，不能合并为独立13图验证集。

| 指标 | 语义7图：统一组合 | 语义7图：本轮 | 历史几何6图：统一组合 | 历史几何6图：本轮 |
| --- | ---: | ---: | ---: | ---: |
| 原始语义像素mIoU | 0.952291 | 0.958359 | 0.950073 | 0.956176 |
| 最终实例类别像素mIoU | 0.902798 | 0.894182 | 0.905809 | 0.902630 |
| 匹配实例mIoU | 0.841199 | 0.840659 | 0.835819 | 0.833798 |
| GT惩罚mIoU | 0.755116 | 0.732747 | 0.717204 | 0.715470 |
| 双向惩罚mIoU | 0.554880 | 0.557950 | 0.465006 | 0.460271 |
| 匹配/GT | 1000/1114 | 971/1114 | 780/909 | 780/909 |
| 预测实例数 | 1516 | 1463 | 1402 | 1413 |
| 铁素体/珠光体预测数 | 979/537 | 941/522 | 850/552 | 828/585 |
| 铁素体均面积相对误差↓ | 28.2080% | 25.3943% | 40.2038% | 38.5500% |
| 内部代理总分 | 77.9559 | 79.3358 | 71.6890 | 72.4149 |

原始语义提升在原标注覆盖区也存在：两组像素mIoU分别由0.983250/0.980185变为0.985822/0.983905，
不只是新GT填缝区改善。但变为最终实例及整粒类别后，像素指标反而下降，说明语义优势尚未转化为最终输出收益。
由于共享LoRA、两头初始化和教师依赖同时变化，不能据此单独归因于affinity训练或某一个融合操作。

七图少匹配29个实例，其中铁素体648→633、珠光体352→338。
铁素体分裂诊断73→65，但合并诊断102→125；额外铁素体预测331→308。
减少碎片与增加合并同时存在，不能把预测数量降低等同于几何改善。
六图匹配总数不变，珠光体额外预测219→252、分裂60→72；仍有局部拓扑代价。

面积改善出现在7图中的5张、6图中的4张；逐图面积相对误差均值也由26.9668%→24.2165%、
38.0473%→36.4339%。本次内部变化不只由一个离群样本推动，但不能据此预测黑盒面积项。
已目检固定四图的原图/新GT/旧组合/本轮对照，整体形态接近，复杂珠光体区域的过切仍明显。

## 权重、产物与后续决策

服务器根目录和产物根目录沿用上文。实际部署权重：

| 文件（相对产物根目录） | 轮次 | SHA256 |
| --- | ---: | --- |
| `adaptation/best_joint_lora.pth` | 35 | `8e4f335427deb0d406dbfa04de5bb936700ad38621f93253a396c44e6ef4eef8` |
| `export/reference.pth` | 原始字段34 | `c224ab6fef5318a821ddfe571064318936ea4c35fedd93f4f93cdd1aced0408f` |
| `geometry/best_affinity.pth` | 57 | `b4e764f14d48f816d9139d1a1dfe21c9e086b6d571c6cf2a8825e30a21b4fe13` |
| `semantic/best_model_stage2.pth` | 14，原始字段13 | `306fa79d5b6c9e0febc7e8ae3c4e2a35f2b3d044c6174feeb66b7ba9f4f3a9b9` |

本地私有分析放在`output/20260919_stage1_final_tasks/`：
`results.json`保存训练完整性、来源、解析配置、逐图与汇总指标，`comparison_summary.json`保存聚合对照；
`semantic_comparison.jpg`和`geometry_comparison.jpg`为四张固定图的目检面板。

68图提交包已生成并下载到本地：
`output/20260919_stage1_final_tasks/submission_stage1_final_s14_g57_top2_high065_20260919.zip`。
大小3,114,243 bytes，SHA256为`1273524922a19586db3444d691ecd0e698ba97894b7699693f5f05eb236e9e51`。
68图/136个平铺文件、CRC、原生尺寸、单通道uint16、实例与类别ID对应全部通过；最大实例ID225。
固定3张测试图在评估入口与批量入口下逐文件一致，本地下载后再次核对格式及整包哈希。
没有提交竞赛平台。完整回执见私有目录的`package_summary.json`及`local_package_check.json`。

68张无标签输出中，总实例6144→5775，铁素体4283→3714、珠光体1861→2061；
铁素体总像素127,489,076→126,241,705（约−0.98%），铁素体实例数约−13.29%。
52/68张预测铁素体平均面积增加，逐图相对变化中位数+8.87%；未分配像素比例的逐图均值为1.3874%→1.3614%。
因此主要变化是实例划分/类别组成，不是大面积丢失覆盖。方向是否更接近真实平均面积未知，
不得用这些无GT数量拟合阈值、强制实例数或断言测试精度提高。
`test68_output_comparison.json`记录无标签变化，固定三图对比为`test3_comparison.jpg`。

回分前按既定s14/g57生成一次官方对照是内部筛查后的决定；回分80.735后拒绝晋级。
统一组合84.925及原S 85.535保留，不因e35为末轮最佳便续训，或同时取消Stage1、加入EMA。
拟议下一步是无训练的2×2输出交换，固定旧/新两套语义与affinity，各用自己的encoder/LoRA前向，
在原生网格语义logits与affinity融合边界处组合；验证两个原始组合复现后，比较两个交叉组合。
其目的为定位部署层面的语义、几何与交互影响，不是隔离joint-v3训练本身的因果作用。
该对照本轮尚未实现、未运行；不直接把旧头挂到新LoRA特征上。

本轮曾通过`dualog-audit`请求Claude Code只读核查上述归因与方案，会话为
`dialog-1789809967533-05a8575d`。沿用用户配置，实际后端ID未回显；等待期间没有返回伙伴消息，
已结束会话并回收子进程，完成审查轮数0。本报告没有伙伴批准或双方共识，结论来自本地代码与产物核验。

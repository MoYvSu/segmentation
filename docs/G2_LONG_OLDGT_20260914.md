# L：原GT、跳过两图暖启动的长程G2

2026-09-14用户授权：先按讨论的分组学习率和长程预算测试G2，继续使用原GT；新GT留到后续独立比较。
本次120轮已于20:53:21正常结束，按预定验证损失选择epoch115，loss `0.20555063088734946`。
20:14:42启动wrapper，20:15:04执行训练命令，含训练进程内准备/评估约38分16秒。
固定E10a/top2/high0.65的六图及三张无标签图比较已完成；L恢复分区能力，内部存在匹配与面积取舍。
2026-09-15用户回报L官方黑盒为`0.8416/0.8538`，折算总分84.770，比X-top2高0.065分。
这次测量支持采用L替代独立G0/G0-long阶段，X保留回退；没有修改默认部署入口或启动新训练。

## 目的与对照

- L：`V6 boundary FPN → 一次连续几何训练`；不上G0/G0-long，不读取其checkpoint。
- 相对Y：初始化、原人工GT、SAM2、损失、增强、冻结参数和划分保持原协议；变化为训练预算和分组学习率。
- 相对X：移除独立两图暖启动并重新分配训练预算；不称为严格等预算或单独证明学习率/时长的因果作用。
- 成绩基准：X epoch16 + E10a/top2/high0.65，用户回报`mIoU=0.8415, 面积项=0.8526, 总分=84.705`。
- 本轮不使用补缝新GT。后续新GT对照应保留本次初始化、学习率、预算、划分和最终推理协议。

## 官方回分与阶段删留（2026-09-15）

来源：用户本任务回报的官方黑盒`(mIoU, 面积项)`。L按本任务已准备的best115/top2包归属，
本地package记录SHA256为`e54542d9352dd383e4514ec58960bbc74799f6759947a25ad75a33268db53e3b`；
未直接取得平台包哈希回执。总分按`50 × (mIoU + 面积项)`计算。

| 方案 | mIoU | 面积项 | 总分 |
| --- | ---: | ---: | ---: |
| X-top2：G0 → G0-long → 原G2，best16 | 0.8415 | 0.8526 | 84.705 |
| L-top2：跳过两段，长程G2，best115 | 0.8416 | 0.8538 | 84.770 |
| L减X | +0.0001 | +0.0012 | +0.065 |

从缩短独立训练链的目标看，本次替代成功：已找到`V6 boundary FPN → 长程G2`的配方，
在这次官方测试上保持X-top2成绩，两项都略高。可以将L作为后续简化实验基线，X保留回退。
删除的是G0/G0-long独立阶段，原两图仍可作为普通人工训练样本；V6初始化、E10a语义及SAM2监督没有被消融。
几何独立运行由三段变为一段，完整训练预算由2780次更新/3560次抽取变为3120/6240；简化不等于减少训练量。

这是已测配方的工程结论，不是统计等效或普遍无收益的证明。单次训练与回分不能估计重复训练波动；
本次也未单独分离初始学习率、参数分组、衰减时程与预算的贡献，不把优化不足判定为历史退步的唯一原因。
旧GT已足以支持本次流程简化，新GT作用仍待独立验证。

内部六图逐图平均代理L比X低2.1677分，合并代理低1.9865分，面积误差五差一好；
官方面积项却高0.0012。此例进一步支持将六图代理用于诊断，不能机械用于阶段淘汰或预测官方排名。

后续新GT对照沿用相同V6起点、随机输出层初始化、120轮分组学习率、原人工验证loss选优与固定top2推理，
只替换训练标签；不接着L续训。本次仅记录回分和决策，不修改代码/默认配置、不连接服务器、不启动训练或推送产物。

## 固定训练协议

配置：[affinity_geometry_g2_direct_long_oldgt.yaml](../config/train/affinity_geometry_g2_direct_long_oldgt.yaml)，
通过`_base`继承既有Y配置，调用`train_affinity_geometry_g1.py`。

| 项目 | 本次值 |
| --- | --- |
| epoch / batch | 120 / 2 |
| 每轮样本抽取 | 52次，允许重复；人工/SAM2各50%概率 |
| 计划总更新 / 总抽取 | 3120 / 6240 |
| geometry FPN初始学习率 | 5e-5，继承V6训练过的边界FPN |
| 上采样/affinity输出模块初始学习率 | 1e-4，随机初始化 |
| 学习率过程 | 同一AdamW内两组参数，各自余弦衰减至5e-6，120轮调度 |
| GT / 划分 | 原人工LabelMe GT，26训练/6验证，seed42 |
| 验证图 | 172、176、349、623、873、888 |
| SAM2 | 复用既有64图无类别几何样本，不新增候选 |
| 损失 | 原G2负连接1.5、困难负连接1.0、gamma2.0、边权归一化 |
| 输入/输出 | 1024/512，letterbox与原在线增强 |
| 可训练模块 | 独立geometry decoder，共39张量、5,670,024参数 |
| 冻结模块 | V6 encoder/LoRA与参考语义，最终评价使用固定E10a |
| 选优 | 每轮原人工验证`val_affinity_loss`，按预定最小值保存best |
| 保存/监控 | best滚动覆盖，latest仅最终轮；不新增中间轮图集或无标签监控 |

输出仍来自相同8通道模型，没有修改推理或checkpoint结构。新配置的`fpn_learning_rate`启用两组优化，
缺省时保留旧的单组参数和学习率。新增CSV列`learning_rate_geometry_fpn`/`learning_rate_geometry_output`
记录该轮实际使用值；旧`learning_rate`列仍是原逻辑中的调度后值，本配置下只指FPN组，
输出模块的当轮学习率应读取`learning_rate_geometry_output`。

## 环境、实现与产物

- 本地分支：`codex/g2-long-oldgt-20260914`，基于`375dbc2`；保留所有既有未提交改动。
- 独立服务器checkout：`/root/autodl-tmp/segmentationv2_g2_long_oldgt_20260914`，基础`f710a9be6a4e2556eacb77b462a3523601ab25fb`。
- 沿用已验证服务器源码，只有训练入口的分组学习率与日志变更；没有整体上传本地旧数据读取实现。
- 环境：`/root/miniconda3/envs/sam2_env/bin/python`，RTX4090。启动前GPU空闲、数据盘约37%。
- 数据和outputs通过原目录链接复用，不复制数据集或基础权重；未改写X/Y/Z运行目录的源码和产物。
- 配置SHA256：`8e1f6403ea17b42fc57d9c825eba71e958725ac962ca3e1d8db3ed590dace88a`。
- 训练入口SHA256：`d84ed8a2118aadf0aafc29c6e334c7bd2967dc3bab304c78a2f9d6447b0403d0`。
- 启动脚本：[run_training.py](../output/20260914_g2_long_oldgt/run_training.py)，SHA256 `c8d0c53fc6274d24f657362ebd58f9ed15be02b13c62771c85e6ebe312dcc72a`。

服务器`outputs/20260914_g2_long_oldgt/`（物理路径位于`segmentationv2_work/outputs/`）：

- `training_status.json`：preflight/training/complete/failed状态，结束时记录best epoch/loss。
- `training_launch.json`、`training_preflight.json`：启动、完整配置、初始化/数据/参数组来源。
- `wrapper.log`、`training.log`：预检和实际训练日志。
- `training/metrics.csv`、`training/selection_summary.json`、`training/split.json`：训练统计与选优证据。
- `training/best_affinity.pth`、`training/latest_affinity.pth`：best及最终轮权重。

本地51项CPU测试通过，覆盖新分组完整无重复、实际前反向及冻结参数、120轮学习率终点、旧单组兼容、
原初始化/严格checkpoint载入和既有affinity损失行为。Python静态编译通过；服务器传输SHA一致。
启动脚本还核对Y的完整继承配置、初始验证loss/正负连接数量、原划分与SAM2身份。

不安排训练后自动推理或持续追踪；用户通知结束后，再以loss-best做双方固定E10a/top2/high0.65的完整部署比较。

## 启动确认

真实V6初始化、原人工26/6划分、64张SAM2批次和可训练参数范围通过预检；未读取G0/G0-long权重。
epoch0原GT验证loss为`0.692930152018865`，与Y预检相同；每个方向正/负连接数量及损失协议字段也完全一致。
两组参数覆盖全部39张量且没有重复：geometry FPN 28张量/5,152,768参数，输出模块11张量/517,256参数。

实际训练日志20:16:46记录初始学习率`5e-5`/`1e-4`，首轮CSV记录：

- epoch1 train_loss：`0.6730176095779126`。
- epoch1 val_affinity_loss：`0.6430194775263468`。
- 本轮实际学习率：FPN `5e-5`、输出 `1e-4`。
- 20:17:00进程状态为training，GPU已分配约5440MiB；此前间隔包含数据准备和epoch0评估。

首轮仅用于确认训练运行，不据其相对Y的下降速度判断最终效果。后续不持续查询。
本地保留[启动核验快照](../output/20260914_g2_long_oldgt/startup_check.json)、
[预检记录](../output/20260914_g2_long_oldgt/training_preflight.json)和启动脚本；`startup_check.json`保留启动时快照，
本轮分析已将`training_status.json`同步为服务器完成状态。

## 训练完成核验

120轮train/val loss均为有限值，逐轮实际分组学习率与预定余弦调度一致；
best/latest的配置、原划分、初始化、冻结语义身份、39个geometry张量与预检一致。
没有加载G0/G0-long，也没有换用新GT；loss-best就是全部已训练轮次中的最小验证损失，没有按部署代理改选。

- best epoch115，loss `0.20555063088734946`，SHA256 `c71a302c97637959fe28d6dc28fc241c06e4dbd8c8090093e98ef955e10595e5`。
- runner-up epoch106，loss `0.2075657695531845`；差值只作描述，不作独立重复实验的显著性推断。
- latest epoch120，loss `0.21094587445259094`，SHA256 `caf3be509d5eee9b3407f63aa4265e2fb2953d4429a467bb8eb6bb0aef85d6f4`。
- 初始loss与Y相同为0.69293015。L截至30/60/90/120轮的最低loss依次为0.310121/0.224344/0.209181/0.205551。
- X loss-best为0.201882，旧短程Y为0.466348。L大幅缩小这一差距，尚不能把损失接近等同于最终实例质量相同。

同为30轮时L已经优于Y的历史loss，说明收益不能全部归为第31–120轮；但前30轮学习率和衰减进度本来就不同，
本次没有单独分离初始学习率、分组学习率和训练时长的作用。
[损失与样本抽取预算对照](../output/20260914_g2_long_oldgt/training_comparison.png)同时展示G2轮数与含暖启动的总抽取量，
不把重复抽取当作独立图像，也不把样本量相同解释为监督来源或训练过程等价。

L完整训练为3120次更新/6240次抽取，入选epoch115对应2990/5980；
X含两图暖启动的完整链为2780/3560，入选G2 epoch16对应2416/2832。
这只排除“L的总更新数比X少”，不证明L已充分优化；不同样本来源、参数组学习率和训练过程仍不可折算成同一种有效预算。

## 固定top2最终部署比较，非官方成绩

使用既有已验证推理checkout`/root/autodl-tmp/segmentationv2_g1_ablation_20260912`，
源码指纹与X/Z时一致；不把本地旧数据读取实现上传覆盖。三组固定E10a、gated/top2、high0.65、low0.45、seal2、
局部重建8步、概率均值投票及受阻分水岭。训练/推理checkout的outputs指向同一物理目录，无checkpoint拷贝。

X复用此前已核验的top2六图与68图输出；L best115和Y best29本轮各完成相同六图和test_001/024/044。
本轮Y也使用top2，不能将它与此前Y-mean的数字混用。评价仍是同一补缝GT、unknown ignore，
在原生尺寸最终输出的已知域计分；GT共909实例（铁素体505、珠光体404），逐图GT审计一致。
所有新结果的checkpoint、完整部署manifest、原尺寸uint16及实例/类别ID对应检查通过。

| 内部指标 | X-top2 | L-top2 | Y-top2 |
| --- | ---: | ---: | ---: |
| 已知域预测数 | 1273 | 1393 | 720 |
| 有效匹配 / 909 | 733 | 777 | 413 |
| 仅有效匹配mIoU | 0.836772 | 0.831006 | 0.812575 |
| GT惩罚mIoU | 0.674757 | 0.710332 | 0.369190 |
| 同时计入多余预测的对称惩罚mIoU | 0.481818 | 0.463526 | 0.369190 |
| 铁素体有效匹配 / 505 | 431 | 444 | 268 |
| 珠光体有效匹配 / 404 | 302 | 333 | 145 |
| 铁素体均面积误差，合并统计 | 30.9320% | 34.3284% | 20.7340% |
| 铁素体均面积误差，逐图平均 | 29.9145% | 33.7500% | 41.9153% |
| 合并代理分 | 76.3726 | 74.3861 | 80.2617 |
| 逐图平均代理分 | 76.9475 | 74.7798 | 69.6864 |
| 已知域未分配像素，逐图平均 | 1.2548% | 1.2883% | 0.9416% |

L相对同top2的Y净恢复364个有效匹配，两类的可匹配分区都大幅恢复。
相对X，六张图的匹配数和GT惩罚mIoU全部提高；净多44个匹配，其中铁素体13、珠光体31。
但预测数也多120个，未匹配预测净多76个；对称惩罚mIoU下降，不能把更高匹配召回写成全面更优几何。
成功匹配集合随模型改变，valid mIoU略低也不直接等于所有对应轮廓都更差。

| 图像 | X匹配 | L匹配 | X面积误差 | L面积误差 | L减X代理分 |
| --- | ---: | ---: | ---: | ---: | ---: |
| train_172 | 129 | 137 | 54.16% | 55.41% | -1.2216 |
| train_176 | 115 | 133 | 43.57% | 41.89% | +1.6621 |
| train_349 | 103 | 106 | 21.13% | 25.48% | -3.1277 |
| train_623 | 108 | 109 | 17.31% | 22.72% | -2.5748 |
| train_873 | 137 | 144 | 18.90% | 29.16% | -5.2252 |
| train_888 | 141 | 148 | 24.42% | 27.84% | -2.5192 |

L的合并mIoU分项比X低0.2883分、面积分项低1.6982分，合并代理低1.9865，逐图平均低2.1677。
面积误差六图五差一好；Y合并代理反而最高，再次说明不能只取有利的聚合口径。
此前Z和X-top2的六图面积方向均与官方相反，因此这里必须呈现面积代价，但不能从它预测L官方得分。

最终类别像素诊断（同批预测的另一统计，非独立数据、非官方实例mIoU）：X/L/Y为0.900874/0.893053/0.740583。
L已接近X，远高于Y；它仍是固定E10a下划分和区域投票的变化，不是语义头被重新训练。
三张无标签图X/L/Y实例数为test_001 `103/102/71`、test_024 `82/77/55`、test_044 `66/65/59`。
目检L与X的主要分区已经接近，Y的部分大面积合并/类别变化得到恢复；局部边界和小分区仍有差异。
数量接近和无标签目检不构成测试集精度证明；图中绿色为铁素体、蓝色为珠光体、白色为实例边界。

- [完整结果、逐图统计与权重身份](../output/20260914_g2_long_oldgt/results.json)
- [测试图对比](../output/20260914_g2_long_oldgt/test_comparison.jpg)
- [验证图对比](../output/20260914_g2_long_oldgt/val_comparison.jpg)

## 复核后补做的mean读出诊断

为检查固定top2是否对L不利，仅将同一L best115的短程归约改为mean，补跑相同六图；
其余完整推理合同、语义权重与GT不变，X-mean沿用此前已核验产物。
这是看过主结果后追加的诊断，不用它重新挑选正式候选读出，也不改变预定top2比较。

| 内部指标 | X-mean | L-mean | X-top2 | L-top2 |
| --- | ---: | ---: | ---: | ---: |
| 已知域预测数 | 1235 | 1361 | 1273 | 1393 |
| 有效匹配 / 909 | 720 | 782 | 733 | 777 |
| 仅有效匹配mIoU | 0.842912 | 0.837530 | 0.836772 | 0.831006 |
| GT惩罚mIoU | 0.667653 | 0.720516 | 0.674757 | 0.710332 |
| 铁素体均面积误差，合并统计 | 28.3474% | 33.9665% | 30.9320% | 34.3284% |
| 合并代理分 | 77.9719 | 74.8933 | 76.3726 | 74.3861 |
| 逐图平均代理分 | 78.2676 | 75.2088 | 76.9475 | 74.7798 |

L从top2换mean，匹配多5个、预测少32个，合并代理提高0.5072、逐图平均提高0.4291；
但X换mean的内部改善更大。相同读出下L减X的合并差从top2的-1.9865变为mean的-3.0786，
逐图平均差从-2.1677变为-3.0587。因此mean没有消除这次六图上的差距，
目前不支持“主要因为选了top2而压低L”的解释；仍未排除其他模型与读出的交互，也不把差距确定为纯模型原因。

[完整读出诊断与manifest](../output/20260914_g2_long_oldgt/readout_check.json)，
[运行脚本](../output/20260914_g2_long_oldgt/check_mean_readout.py)。仅新增六图输出，没有扫阈值或改模型。

## 2026-09-14回分前的判断与下一步

调整优化配方后，原GT单阶段训练能够恢复到值得和X做官方比较的水平；这项结果支持继续简化独立训练环节。
它不证明两图预训练没有收益，也未单独分离初始学习率、分组、衰减和训练预算的贡献。
L与X仍是召回/多余分区/面积的取舍，保留X-top2 84.705分的基准，L准备一次官方核验候选。
不能因为L局部指标改善就宣布G0/G0-long正式删除，也不能因为六图代理降低而直接淘汰它。

下一项新GT对照值得做，但应从与L相同的V6 FPN和随机输出层起点重新训练，固定120轮、分组学习率、
采样比例、损失权重、增强、seed与最终top2推理，只替换26张训练图的人工监督目标。
为单独检查训练GT的作用，建议保留与L相同的原人工验证loss选优，最终评价也沿用相同补缝GT；
新GT验证loss可以另作诊断，不与原GT历史loss直接比较或混用选优。接着L权重续训会同时增加训练预算，不是该对照。
新GT将如何改变粒内正连接与跨实例负连接仍需统计；本轮不启动该训练。

## L官方核验候选包

- [submission_L_best_e115_top2_high065_20260914.zip](../output/20260914_g2_long_oldgt/submission_L_best_e115_top2_high065_20260914.zip)
- best epoch115，固定E10a/gated-top2/high0.65及上文全部后处理，不挑图、不按图改参数。
- 68张图、136个平铺文件；原尺寸单通道uint16、ID上限和类别映射检查通过。
- 三张probe的PNG/JSON与本次完整68图输出逐文件字节一致；服务器和下载后ZIP CRC/SHA核验通过。
- 大小3,156,333 bytes；SHA256 `e54542d9352dd383e4514ec58960bbc74799f6759947a25ad75a33268db53e3b`。
- 全68图L预测总数5984，X-top2为6134；这与六图已知域预测数增加的方向不同，不能把六图的额外分区趋势直接外推到测试集。
- [manifest及服务器格式校验](../output/20260914_g2_long_oldgt/package.json)、[本地传输核验](../output/20260914_g2_long_oldgt/local_package_check.json)。

该包用于核验是否能在省去两图独立训练环节的同时保留竞赛表现；生成时未代用户提交。
2026-09-15已收到用户回报的L成绩，见本文回分节；本次记录没有切换默认部署入口。

## 与大肥鱼的两轮只读复核

使用dualog-audit，session `dialog-1789390967427-21037218`；Claude Code入口，
本次读取的配置为DeepSeek `deepseek-flash[1m]`，API入口`https://api.deepseek.com/anthropic`。
伙伴读取仓库代码、配置和本地产物；没有让伙伴SSH、修改代码或训练。会话已结束，属于实验讨论，不是形式化代码审批。
[原始讨论归档](../output/20260914_g2_long_oldgt/dualog_discussion.json)保留完整发言，不能将所有发言都当作确认后的结论。

共同结论：本次配方明显恢复原GT直接训练的分区能力，L值得一次官方核验，X-top2继续作为成绩基准；
只删除独立暖启动环节的目标仍值得探索，后续新GT必须独立比较，不接着L续训或同时改变推理。
伙伴提出检查mean敏感性，已补做上文对照；其初次审查未发现本次训练或固定top2评价的具体实现错误。

讨论中明确纠正或未采纳的推断：

- L更新数更多不证明充分优化；不能把未消除的差距排除为训练预算/过程问题。
- Y的69.6864是逐图平均代理，合并代理是80.2617；不得与L合并74.3861混写。L相对Y的恢复有匹配、惩罚IoU、
  类别像素诊断和目检支持，但合并代理排序仍相反；“L在所有代理口径下最差”也不成立。
- 过去代理与官方的偏差不能称为统计误差带，不能预测L官方与X相同或更高；保留逐图面积代价作为诊断。
- 相邻epoch与runner-up差不能估计独立重复实验噪声，不据此改成固定epoch选优；也不从raw组件数判断loss-best选错。
- 原始图评估与最终部署同时改变读出、后处理、评价网格和GT，不能把排序差只归于补缝GT。
- 未来保持原人工验证loss选优，是冻结本次选优规则；训练GT替换自然改变监督连接集合，并非额外独立变量。
  这种设计能检查固定流程下替换训练标签的效果，但不会保证对每种GT都达到最优；不预设偏差方向或改善幅度。
  L本次最终评价也使用补缝GT，不存在“L训练/选优/最终评价三项都用原GT”的事实。

伙伴第二轮认可主要决策与上述预算、选优、聚合口径和证据强度修正；其余未经验证的外推不作为本报告结论。
没有增加latest部署、阈值扫描或新GT训练；本轮实际新增验证只有固定top2的L/Y部署、68图L包和六图L-mean诊断。

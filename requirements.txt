# =============================================================================
# Metallographic Image Phase Segmentation — Python Dependencies
# =============================================================================
# Install into a fresh conda environment:
#   conda create -n sam2_env python=3.11 -y
#   conda activate sam2_env
#   pip install -r requirements.txt
#
# NOTE: PyTorch wheels are CUDA-version specific. The lines below pull the
# default (CPU) build. For GPU support, visit https://pytorch.org/ for the
# correct --index-url for your CUDA version, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# =============================================================================

# ---- Deep Learning Framework ----
torch>=2.8.0
torchvision>=0.23.0

# ---- SAM 2 (segment-anything-2) transitive deps ----
# SAM 2 is a local submodule under segment-anything-2/, not a pip package.
# These are the third-party packages it imports at runtime.
hydra-core>=1.3.2
omegaconf>=2.3.0
iopath>=0.1.10
tqdm>=4.66.0

# ---- Scientific / Image Processing ----
numpy>=1.26.0
opencv-python>=4.10.0
Pillow>=10.0.0

# ---- Machine Learning (Stage 2 classifier) ----
scikit-learn>=1.4.0
joblib>=1.4.0

# ---- Debug Visualization (optional, used by render scripts) ----
matplotlib>=3.8.0
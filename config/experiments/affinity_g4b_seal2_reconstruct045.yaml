_base: affinity_border_seal2.yaml

# Inference-only: retain the G4b 0.72 high barrier, while marker topology may
# grow through >0.45 weak responses for at most eight native-resolution pixels.
inference:
  boundary_threshold: 0.72
  marker_boundary_low_threshold: 0.45
  marker_boundary_reconstruction_steps: 8


# 低碳钢金相图像无监督相区分割：规则备忘

> 本文件用于项目内快速查阅，不替代组委会最终通知、QQ群答疑或实际下发的数据包。

## 信息来源与口径

- 本地赛题 PDF/TXT：仓库根目录中的赛题文件。
- 官方公开页面：[2026AIC·“AI＋钢铁”低碳钢金相图像无监督相区分割](https://www.aicomp.cn/tracks/tracks-6/4185.html)
- 实际数据口径（由参赛者根据当前下发数据确认）：
  - 无标签训练图像共 **1000 张**；
  - **没有**分割标注示例；
  - 允许自主标注的训练图像数量 **小于 500 张**（实践中预计只标注远少于上限的高价值样本）。

官方公开页面目前仍出现“500 张无标签、最多标注 250 张、提供 1 张示例”等旧口径，且页面不同段落还出现 250/500 的内部冲突。项目实施时以上述实际数据口径和组委会最新通知为准。

## 核心任务与限制

- 输出类别感知的实例分割：
  - 珠光体（Pearlite）类别为 `0`；
  - 铁素体（Ferrite）类别为 `1`。
- 每个独立铁素体晶粒、每个连续珠光体团簇均应具有独立实例编号。
- 实例掩码为与原图同尺寸的 `uint8` 单通道 PNG：`0` 为背景/忽略，`1..255` 为实例编号。
- 每张图最多 **255 个实例**；同时输出实例编号到类别的 JSON 映射。
- 仅可使用赛方无标签训练图像和合规的自主标注训练数据；不得使用其他金相数据集、外部标签或测试集标签。
- 可使用 SAM 等通用模型的特征或初始化，但不得使用预训练的金相相区分割权重。
- 训练方式应属于无监督或半监督；模型参数量小于 `500M`。
- 输入至少包含 `1244×1044` 和 `2448×2048` 两种分辨率，提交结果必须恢复到原图尺寸。

## 两项客观评分指标

### 1. 类别感知的实例 mIoU（50 分）

1. 只在类别相同的真实实例和预测实例之间匹配。
2. 采用一对一匹配：一个真实实例和一个预测实例都最多参与一组匹配，依据为 IoU 最大。
3. 只有 `IoU >= 0.5` 的配对属于有效匹配。
4. 对有效匹配集合计算：

   `IoU_i = |P_i ∩ G_i| / |P_i ∪ G_i|`

   `mIoU_inst = sum(IoU_i) / N`

5. 指标得分：

   `score_mIoU = 50 × mIoU_inst`

网页可读文本没有明确写出 `N=0` 及并列最大 IoU 的处理方式；本地复现评估器应固定这些边界行为，并与组委会答疑核对。

### 2. 铁素体平均晶粒面积（50 分）

该指标只统计 **铁素体实例**，珠光体不参与平均面积计分。

设真实铁素体实例数为 `M`，预测铁素体实例数为 `K`：

`mean_area_gt = sum(area_gt_j) / M`

`mean_area_pred = sum(area_pred_k) / K`

`relative_error = |mean_area_pred - mean_area_gt| / mean_area_gt`

`score_area = 50 × max(0, 1 - relative_error)`

综合客观原始分：

`score_total = score_mIoU + score_area`

## 对本项目评估的直接约束

- 语义 mIoU、像素边界 IoU和 monitor 热图只能作为诊断量，不能代替官方实例 mIoU 与铁素体平均面积进行 checkpoint 选择。
- 欠分割和过分割会同时改变实例匹配与铁素体平均面积；仅观察总实例数无法判断优劣。
- 珠光体需要保证实例 mIoU，但不应为了铁素体面积指标而被边界后处理过度切碎。
- 每张图必须严格满足 255 实例上限；若超限，应采用可解释的同类相邻实例合并策略，而不是按固定目标实例数调整。
- 测试集可用于提交格式检查和不依赖标签的部署诊断，但不得进入训练、伪标签、人工标注或有监督模型选择流程。

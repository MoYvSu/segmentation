# AGENTS.md

本文件只保存适用于整个仓库、且不应随单次实验频繁变化的代理规则。项目介绍、当前最佳结果、
命令和模块清单不要复制到这里。

## 事实来源

- 开始工作前先检查当前 checkout、未提交改动和与任务相关的配置/文档；不要用旧会话结论覆盖
  当前工作区证据。
- 当前主线、实验状态和推荐入口以 `README.md`、`docs/PIPELINE.md` 与 `config/README.md`
  为准；赛题合规边界以 `COMPETITION_RULES.md` 为准。
- 具体实验只读取对应的 `docs/*EXPERIMENT*`、`docs/AFFINITY_*` 或其他专题文档，不在本文件
  维护成绩和 checkpoint 快照。
- 文档、配置和代码冲突时，先核对实际代码与产物；不得静默选择其中一个，也不得把未核验状态
  写成已完成事实。

## Skill 路由

- 项目级 Skills 位于 `.agents/skills/`。任务与某个 Skill 的 `description` 匹配时，按需读取其
  完整 `SKILL.md`，并且只加载当前工作所需的 references；不要把 Skill 全文复制到本文件。
- 对任务范围、修改必要性、额外重构、新依赖、新抽象层或其他 scope creep 存疑时，优先使用
  `stop-that-shit` 判断请求内容、必要后果与可达证据。这里只安装 advisory Skill，没有 Guard、
  Hook 或 Plugin，不得声称存在机器拦截。
- 工作中产生可能跨任务复用的实验教训、环境问题、代码库特性或有效/无效方案时，使用
  `maintain-project-knowledge`；候选知识先写入该 Skill 的 `references/`，不得因单次事件直接
  修改 `AGENTS.md`。
- 只有经过证据验证、仍符合当前项目、具有跨任务价值且属于项目全局约束的知识，才在专门的
  规则维护中精炼后晋升到 `AGENTS.md`。流程类经验保留在相应 Skill；失效经验应退休或删除。

## 优化目标与晋级规则

- 第一目标是赛题口径的铁素体/珠光体 mIoU 与铁素体平均面积得分。实例数量、训练损失、
  reconstruction loss、Oracle graph 指标和单个早期 epoch 只能作诊断。
- 候选模型必须在 README 所列稳定部署链上做完整最终输出比较；不得凭张量初始化、训练集改善、
  Oracle 指标或无标签目检单独替换已验证主线。
- 实验应尽量保持单变量和可回退。改变语义头、LoRA、geometry、fusion 或后处理时，明确列出
  唯一变化和固定项；不要未经证据叠加 graph、RAG、形状规则或多模型连续融合。
- 残差实验必须验证“最终部署输出”的零残差兼容性，而不只验证残差张量为零。若 LoRA 或共享
  encoder 发生变化，不得默认复用依赖旧特征的 decoder/geometry。
- 测试集可用于无标签目检和合规后处理分析，但不得制作、推断或人工修改测试 GT/答案，也不得
  通过强制实例数或平均面积拟合提交结果。

## 数据与模型硬约束

- 语义约定：珠光体为 `0`，铁素体为 `1`；实例图每张的最大 ID 必须 `<=255`。
- 总模型参数量必须 `<500M`。FPN/任务 decoder 随机初始化，禁止加载 SAM2 原生 Mask Decoder
  权重。
- 只使用赛方允许的数据；标注数量、伪实例源图上限和其他数据合规要求严格遵守
  `COMPETITION_RULES.md` 与 `docs/sam2_pseudo_instance_dataset.md`。
- SAM2 mask 默认只是无类别几何候选。未覆盖像素和 covered-uncovered pair 保持 ignore；
  自动候选在进入有监督训练前必须经过约定的互斥处理和人工确认。
- 图像几何变换统一使用等比 letterbox + reflect padding，禁止挤压。增强在线完成，不离线改写
  原始图像或标签。
- 权重与第三方模型放在项目约定目录中；禁止依赖用户级全局缓存或把本机/服务器绝对路径写死
  到已跟踪配置。

## 实现与兼容性

- 保持 checkpoint 向后兼容：新增键使用显式版本或兼容读取；加载日志中的 missing/unexpected
  keys 必须核对，不能用宽松加载掩盖架构错误。
- 配置继续使用现有 `_base` 继承和项目路径解析机制。新增依赖时同步 `requirements.txt`。
- Python 文件保留 `# -*- coding: utf-8 -*-`；注释、实验说明和提交信息优先使用中文，提交信息采用
  `<type>: 描述`。
- 不修改与当前任务无关的用户改动，不覆盖本地数据、权重、outputs 或未跟踪分析产物。

## 验证与运行

- 修改后执行与风险相称的最小测试集，并优先验证最终输出契约、shape、冻结参数、checkpoint
  加载和 `<=255` 限制。smoke 通过不等于竞赛增益。
- 本地主要做静态检查、CPU smoke 和小规模推理；需要实际 GPU 训练时使用项目规定的
  `sam2_env` 训练环境。资源不可用时记录阻塞点，不用本地代理结果冒充正式训练。
- 报告实验结果时同时给出配置、checkpoint 路径、epoch、关键哈希/版本和完整部署指标，区分
  官方黑盒、完整代理、Oracle、训练指标与目检结论。

## Git 与文档

- 新分支默认使用 `codex/` 前缀。每个实验保持可独立回退，避免把不相关改动混入同一提交。
- Windows 下若 Git 元数据写入失败，先只读检查 `.git` 状态和锁文件；未经明确授权不得修改
  owner/ACL，也不得删除可疑锁文件。
- 可复用的新结论写入最相关的 README/docs；这里只增加长期有效的代理行为规则。完成修改后
  检查聚焦 diff 和 `git status`，并明确说明未执行的验证。
